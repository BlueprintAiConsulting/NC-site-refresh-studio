# Supabase Configuration (optional - site runs without it)
# Get these from Supabase Dashboard → Project Settings → API
# Use the "anon" public key only (NOT the service_role key)

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Google Calendar API (optional)
# 1. Go to Google Cloud Console (console.cloud.google.com)
# 2. Create/Select project → Enable Calendar API
# 3. Create API Key → Restrict to Calendar API
# 4. Get Calendar ID from Google Calendar settings (usually your-email@gmail.com or calendar-id@group.calendar.google.com)

VITE_GOOGLE_CALENDAR_API_KEY=your-api-key-here
VITE_GOOGLE_CALENDAR_ID=your-calendar-id@group.calendar.google.com
